defmodule Bugsnag.SanitizerTest do
  use ExUnit.Case
  import ExUnit.CaptureLog

  test "the sanatizer defaults to not doing anything"

  test "the sanitizer will run the provided functions"

  test "if the sanitizer throws, then we can continue"

end
